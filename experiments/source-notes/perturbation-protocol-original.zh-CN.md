# 随机站位扰动：18次配对实验执行方案

2026-09-20，用户授权自动完成全部试验并自动分析，暂不写完整报告，以便视结果补实验。固定XLeRobot、I0资料条件，对比E0和E3。沿用gpt-6-astra / xhigh；单次1800秒任务预算、1980秒代理含收尾预算。

## 冻结条件

- 以原站位为圆心，10/50/100cm各三个已选随机点，同档最小夹角60°；positions.json记录坐标、种子及拒绝采样。朝向、初始关节姿态、场景、按钮和控制接口不变。
- 原始body-experience-001的I0E0、I0E3输入目录直接只读复用；PROMPT/API/PRIOR/经验哈希不变。E3始终来自be001-source-3，不从本轮任务更新经验或skill。
- 每个点位E0/E3各一次，共18次。三个点不是同点三次重复；各半径方向独立抽样。分析保留同点配对，并报告方向和随机执行波动混杂。
- 新站位分别生成私有reference，在独立初始化中验证qpos/qvel/ctrl及目标相对坐标一致。私有坐标、研究纪要、点位渲染都不进入受试输入。
- 中间电梯UP是唯一目标；任意非目标按钮被按都算失败，即使随后按对。失败保留，不按结果重采点或重跑。

## 顺序

manifest.json是唯一运行清单。先R010-P1(E0→E3)、R050-P1(E3→E0)、R100-P1(E0→E3)，共六次用于预算校准。其余点位用固定seed20260921打乱，点内继续交替E0/E3顺序；不依据成绩更改顺序。全部独立新会话，逐次运行，共享实验排他锁。

## 自动运行与额度

position_perturbation_study.py负责冻结验证、启动、等待退出与录像归档、汇总、首六次预算校准，再继续剩余任务。逐次检查账号、模型和effort、磁盘及冻结输入。额度来自账号共享整数百分比，六次差值只作粗预算；余额<=10%时等待，不启动下一次，余额恢复后自动接续。额度查询网络错误后台重试，账号或模型改变则停止审计。不使用重置券、不降effort、不购买额度。运行中任务不会因为预算预测变差而主动中止。运行/录像超时或其他基础设施故障保留原尝试并进入needs_audit，不悄悄重跑。

## 结果产物

每次保存三版录像、原始状态、公开动作/观测日志与受试报告。自动更新reports/batches/position-perturbation-001/summary.json和SUMMARY.zh-CN.md，列各档成功率、成功时间、1800秒失败封顶均值、动作/观测数、误按数及逐点配对差。零扰动参考使用已完成body-experience-001的I0E0/I0E3，不当作同点配对重复。

所有18次结束后自动解码54个标准录像，生成成功/失败图像索引与拼图。定时代理视觉复核并保存简短观察（不写完整报告），区分轨迹复用、在线修正与尚未证实的泛化，记录样本量和方向限制。完成后Git保存并暂停定时任务。E2、第三人称示范、skill训练或额外新样本不自动追加。

## 用户后续修正：静默无人值守

用户要求取消预算校准和主动进展报告，继续完成全部试验及简要汇总。quiet-amendment.json与position_perturbation_quiet.py移除首六次预算投影；点位/顺序/受试输入/模型effort保持冻结。adopt_perturbation_quiet.py只在当前试验自然结束且导出完成后接替旧调度器，不中断受试。逐次可用额度及10%余量检查保留；无需用户即时确认，等待恢复后继续。定时任务仅后台检查、处理异常、最终视觉复核与简短分析，不主动发进展通知。
